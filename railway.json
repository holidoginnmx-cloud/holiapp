{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx prisma generate --schema=packages/db/schema.prisma"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema=packages/db/schema.prisma && npm --workspace=@holidoginn/api run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
