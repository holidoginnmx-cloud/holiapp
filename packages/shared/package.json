{
  "name": "@holidoginn/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Reglas de negocio compartidas de Holidog Inn (precios, tallas, desglose, tipos). El admin web y el sitio la consumen como tarball en vendor/ (ver scripts/sync-shared.sh en la raíz del monorepo).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "react-native": "./src/index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.2.7"
  }
}
